<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anew.news">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".news.app.NewsApplication">
        <activity android:name=".news.activity.SplashActivity" >




        </activity>
        <activity android:name=".news.activity.NewsDataActivity"/>
        <activity android:name=".news.activity.WizardActivity"/>
        <activity android:name=".news.activity.MainActivity"
                  android:theme="@style/StyledIndicators">
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>
        </activity>


    </application>

</manifest>